import Prism from 'prismjs';
import 'prismjs/components/prism-toml';

// This component registers the TOML language for syntax highlighting
// The import of 'prismjs/components/prism-toml' automatically registers
// the TOML language definition with Prism

// We're exporting an empty object as Docusaurus theme modules
// need to have an export
export default {};
